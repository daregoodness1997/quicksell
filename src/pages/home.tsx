import React from 'react';
import { TopNav } from '../components';
import Hero from '../views/Hero';

const Home = () => {
  return (
    <div>
      <Hero />
    </div>
  );
};

export default Home;
